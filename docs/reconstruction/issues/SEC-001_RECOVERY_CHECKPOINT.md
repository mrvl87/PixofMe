# SEC-001 Recovery Checkpoint

## Recovery status

BLOCKED — Repository state inconsistent

The approved contract and its expected branch still exist, and the uncommitted implementation appears contract-aligned, but the working tree is currently attached to `chore/agentic-foundation` at the parent foundation commit instead of `fix/sec-001-homepage-upload` at the approved contract commit. Independent QA must not begin from this branch state.

## Audit date

2026-07-16 (Asia/Jayapura)

## Current branch

`chore/agentic-foundation`

Expected by the approved contract: `fix/sec-001-homepage-upload`.

## Current HEAD

`71176a59dd1e4b7c00e554065e596752800cd054` — `chore: establish agentic reconstruction foundation`

HEAD is the parent of the approved contract commit, not a descendant of it.

## Foundation checkpoint

- Commit `71176a59dd1e4b7c00e554065e596752800cd054` exists and is a commit object.
- It is the current HEAD and is contained by both `chore/agentic-foundation` and `fix/sec-001-homepage-upload`.
- `git merge-base --is-ancestor <foundation> HEAD` passed.

## Approved contract checkpoint

- Commit `8f3c7620d907c221e683e893ecb7e81b6b091fcd` exists and is a commit object.
- Branch `fix/sec-001-homepage-upload` exists and points exactly to that commit.
- The commit contains `docs/reconstruction/issues/SEC-001_EXECUTION_CONTRACT.md` with status `Approved — Ready for Implementation` and approved Option C.
- The contract commit is not an ancestor of the current HEAD; `git merge-base --is-ancestor <contract> HEAD` failed.
- The contract file is consequently absent from the current branch checkout. This is branch-topology drift, not a separately recorded working-tree deletion.

## Working-tree state

Before this checkpoint was created:

```text
 M app/api/storage/upload/route.ts
 M prototype/wizard-step4.html
?? app/api/storage/upload/verify-SEC-001.mjs
?? docs/reconstruction/issues/SEC-001_IMPLEMENTATION_EVIDENCE.md
```

No file was staged. `tsconfig.tsbuildinfo` was generated by this audit's TypeScript command, was absent from the initial status, and was explicitly removed after validation. Ignored `.next/` was present/refreshed by the build and was retained because its pre-audit existence could not be proven.

## Preserved user-owned changes

`prototype/wizard-step4.html` remains modified and unstaged. It was not edited, saved, staged, restored, checked out, stashed, deleted, moved, or included in any commit by this recovery audit.

## Commits after contract approval

None. `fix/sec-001-homepage-upload` still points at the approved contract commit. The current branch is one commit behind that checkpoint rather than ahead of it.

## Files changed since approved contract

| File | Classification | State |
|---|---|---|
| `app/api/storage/upload/route.ts` | Approved SEC-001 implementation | Modified, unstaged |
| `app/api/storage/upload/verify-SEC-001.mjs` | Approved route-local dependency-free SEC-001 verification | Untracked |
| `docs/reconstruction/issues/SEC-001_IMPLEMENTATION_EVIDENCE.md` | Approved SEC-001 evidence | Untracked |
| `prototype/wizard-step4.html` | Known user-owned change | Modified, unstaged and preserved |
| `docs/reconstruction/issues/SEC-001_EXECUTION_CONTRACT.md` | Branch-topology inconsistency | Present in contract commit/expected branch, absent from current foundation branch |

No additional source or documentation drift was found. Generated `tsconfig.tsbuildinfo` was removed; ignored `.next/` was not removed.

## Upload route implementation state

The existing unstaged route diff implements the approved fail-closed shape:

- authentication precedes multipart parsing and service-role construction;
- anonymous requests return `401`;
- missing/empty/non-string `kind` returns `400`;
- every `homepage-*` kind returns `403` for authenticated actors;
- only exact `report-photo` is allowed and every other kind returns `400`;
- client `bucket` or `path` fields return `400`;
- the private report bucket is selected server-side with `getSupabaseStorageBucket()`;
- the complete owner-prefixed report path is constructed server-side with a fixed folder, optional ownership-verified project, date, UUID, sanitized name, and MIME-derived extension;
- project ownership, MIME allowlist, 15 MB limit, UUID names, `upsert:false`, response shape, metadata insertion, and cleanup remain present;
- signed-URL and metadata failures both attempt object cleanup;
- dependency errors are generic and do not expose raw provider errors or upload targets.

The implementation is uncommitted and attached to the wrong current branch. It was inspected only; this audit did not modify it.

## Active caller compatibility

Repository search found one active caller at `app/pixforme.tsx`. It sends `kind=report-photo`, sends neither `bucket` nor `path`, posts to `/api/storage/upload`, and consumes the returned `photo` payload. No active homepage uploader or second upload caller was found in `app/` or `lib/`.

## Implementation evidence state

`SEC-001_IMPLEMENTATION_EVIDENCE.md` exists untracked and is detailed. Its static/code gate claims were reproduced by this audit. It explicitly records that no manual Step 3 upload, live HTTP actor matrix, or authorized non-production storage/database side-effect inspection was performed.

Its declared branch `fix/sec-001-homepage-upload` no longer matches the current branch because the working tree was subsequently checked out to `chore/agentic-foundation`. Its `Implemented — Awaiting Independent QA` status is therefore not currently actionable. The branch association and required runtime-evidence disposition must be resolved before QA.

## QA evidence state

`SEC-001_QA_REPORT.md` does not exist. No repository evidence shows that independent QA or red-team work has started.

## Release evidence state

`SEC-001_RELEASE_EVIDENCE.md` does not exist. Release work has not started and is not authorized at this stage.

## Validation results

| Command | Result |
|---|---|
| `git diff --check` | PASS, exit 0; only LF-to-CRLF warnings for the route and preserved prototype file |
| `npm run lint` | PASS, exit 0; 0 errors and 11 pre-existing warnings outside the upload route (9 `no-img-element`, 2 `exhaustive-deps`) |
| `npx tsc --noEmit` | PASS, exit 0; no diagnostics |
| `node app/api/storage/upload/verify-SEC-001.mjs` | PASS, 15 scripted static assertions |
| `npm run build` in restricted sandbox | Environment failure after successful compile/TypeScript start: `Error: spawn EPERM` |
| `npm run build` outside restricted sandbox | PASS; Next.js 16.2.9 generated 29 routes/pages and included `/api/storage/upload` |

No HTTP integration, browser Step 3 smoke test, live database test, or live/non-production bucket mutation was run because this task is a non-destructive recovery audit and no authorized test environment was supplied.

## Scope drift findings

1. Critical workflow drift: the current branch is `chore/agentic-foundation`, so the approved contract is not in the current HEAD even though its branch and commit remain intact.
2. The route, route-local verifier, and implementation evidence are within approved SEC-001 paths.
3. `prototype/wizard-step4.html` is the known user-owned change and remains preserved.
4. No unrelated application, schema, migration, RLS, auth, payment, dependency, environment, UI, or report file change was found.

## Documentation consistency findings

- The approved contract commit is available and its Option C rules match the actual route diff.
- The implementation evidence accurately describes the route, caller, scripted assertions, lint warnings, TypeScript result, restricted `spawn EPERM`, and successful out-of-sandbox build.
- The implementation evidence's branch and `Awaiting Independent QA` status are stale relative to the current checkout.
- The evidence clearly discloses missing manual/runtime and live side-effect verification; it does not falsely claim those tests ran.
- No QA PASS or release evidence exists, so no out-of-order QA/release claim was found.

## Completed milestones

- Agentic reconstruction foundation: completed and committed.
- SEC-001 execution contract: approved and committed on the expected branch.
- SEC-001 implementation candidate: present, statically contract-aligned, and compile-gate verified, but uncommitted on the wrong current branch.
- Independent QA: not started.

The last durable, correctly branched milestone is the approved SEC-001 execution contract.

## Current workflow stage

`BLOCKED — Repository state inconsistent` between implementation and QA. It must not be classified as `Ready for independent QA` until the working tree is safely re-associated with the approved contract branch and the missing runtime-evidence requirement is completed or explicitly dispositioned by the human owner.

## Recommended next agent

Lead Orchestrator, with explicit human authorization for branch-state recovery. After the expected branch is restored and verified, hand the existing diff back to the Billing/Security Agent only if implementation/runtime evidence still needs completion; otherwise perform a fresh handoff to QA/Red-Team.

## Exact next workflow

```text
Human reviews this recovery checkpoint
→ explicitly authorizes preservation-safe branch recovery
→ Lead Orchestrator switches to fix/sec-001-homepage-upload without stash/reset/restore and without touching prototype/wizard-step4.html
→ verify HEAD is 8f3c762 and SEC-001_EXECUTION_CONTRACT.md is tracked/visible
→ verify the only dirty entries are the existing route, route-local verifier, implementation evidence, preserved prototype change, and this checkpoint
→ Billing/Security Agent continues only from the existing diff to complete or formally disposition the required authorized non-production/manual Step 3 evidence
→ re-run diff-check, lint, TypeScript, build, and route-local verification
→ if implementation evidence is complete and no scope drift remains, open a new Codex chat for QA/Red-Team independent audit
```

No branch operation is authorized by this audit itself.

## Allowed next write paths

During branch-state recovery: no file writes; only an explicitly authorized branch switch and read-only verification.

If handed back to Billing/Security after the branch is corrected:

- `app/api/storage/upload/route.ts`
- `app/api/storage/upload/verify-SEC-001.mjs`
- `docs/reconstruction/issues/SEC-001_IMPLEMENTATION_EVIDENCE.md`

QA may later create only `docs/reconstruction/issues/SEC-001_QA_REPORT.md` after a clean handoff.

## Stop conditions

- Any branch switch would overwrite, stage, restore, stash, or otherwise alter `prototype/wizard-step4.html` or another dirty file.
- HEAD does not become the approved contract commit on `fix/sec-001-homepage-upload`.
- The contract file is missing after branch recovery.
- Any additional caller omits explicit `kind=report-photo`.
- Any new helper, schema, migration, RLS, storage-policy, environment, dependency, UI, auth, payment, or shared-contract change appears necessary.
- A secret, production payload, bucket object, or sensitive session value appears.
- Required runtime side-effect evidence cannot be run safely or cannot be explicitly dispositioned by the human owner.
- QA finds a category, bucket, path, ownership, cleanup, or privileged-call bypass.

## Recovery risks

- Uncommitted implementation files are presently attached to a branch that does not contain their approved contract.
- A careless checkout/stash/reset could lose or contaminate the implementation or the protected prototype change.
- Static assertions do not prove actual HTTP status/side-effect behavior against Supabase.
- Residual SEC-005, SEC-007, SEC-012, and SEC-016 risks remain out of scope.
- Existing ignored `.next/` contents may reflect multiple validation runs; they are not release evidence.
